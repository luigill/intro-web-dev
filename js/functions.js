// Função -> Trecho de código reutilizável

// Função Padrão
function soma(a, b) {
  return a + b;
}

console.log(soma(2, 5));

// Função Anônima
(function () {
  alert("hello");
});

// Arrow Function

const mult = (a, b) => {
  return a * b;
};

console.log(mult(10, 2));
